{{> layout/header}}
<div class="container">
    {{#sessionUser}}
        <!--개인 로그인시-->
        {{#isCheck}}
            <!-- 이미지를 표시하는 부분 -->
            <div class="mb-3 mt-3" style="width: 400px">
                <img src="/upload/{{product.img}}" alt="Product Image" style="max-width: 100%; max-height: 100%;">
            </div>

            <div class=" pr-5">
                <div class="mb-3 mt-3">상품명 : {{product.name}}</div>
                <div class="mb-3 mt-3">상품가격 : {{product.price}}</div>
                <div class="mb-3 mt-3">
                    상품수량 : <input name="qty" name="productQty" type="number" value="{{product.qty}}">
                </div>
                
                <span><a href="/order/orderList" class="btn btn-warning">구매하기</a></span>
            </div>
        {{/isCheck}}
        <!--개인 로그인시-->

        <!--판매자 로그인시-->
        {{^isCheck}}
            <!-- 이미지를 표시하는 부분 -->
            <div class="mb-3 mt-3" style="width: 400px">
                <img src="/upload/{{product.img}}" alt="Product Image" style="max-width: 100%; max-height: 100%;">
            </div>

            <div class="mb-3 mt-3">상품명 : {{product.name}}</div>
            <div class="mb-3 mt-3">상품가격 : {{product.price}}</div>
            <div class="mb-3 mt-3">상품수량 : {{product.qty}}</div>

            <div class="d-flex pr-5">
                <span><a href="/product/{{product.id}}/updateForm" class="btn btn-warning">수정가기</a></span>
                <span>
	            <form action="/product/{{product.id}}/delete" method="post">
		            <button class="btn btn-danger">삭제하기</button>
	            </form>
            </span>
            </div>
        {{/isCheck}}
        <!--판매자 로그인시-->
    {{/sessionUser}}

    {{^sessionUser}}
        <div class=" pr-5">
            <div class="mb-3 mt-3">상품명 : {{product.name}}</div>
            <div class="mb-3 mt-3">상품가격 : {{product.price}}</div>
            <div class="mb-3 mt-3">
                상품수량 : <input name="qty" name="productQty" type="number" value="{{product.qty}}">
            </div>
            <span><a href="/order/1/orderList" class="btn btn-warning">구매하기</a></span>
        </div>
    {{/sessionUser}}

</div>
{{> layout/footer}}